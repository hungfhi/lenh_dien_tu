import styled from "styled-components";

const TitleModal = ({
    profile,
    definitions,
    uuid,
    objOrder,
    className
}) => {
    return (
        <div className={className}>
            Modal Role
        </div>
    )
}

export default styled(TitleModal)`
`;